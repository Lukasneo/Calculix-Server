services:
  calculix-server:
    build: .
    image: calculix-server:latest
    ports:
      - "8080:8080"
    environment:
      - CCX_THREADS=8
    volumes:
      - ./data:/data
    restart: unless-stopped
