services:
  calculix-server:
    build: .
    image: calculix-server:latest
    ports:
      - "8080:8080"
    environment:
      - CCX_THREADS=8
      - UPLOAD_LIMIT_GB=1
    volumes:
      - ./data:/data
    restart: unless-stopped
